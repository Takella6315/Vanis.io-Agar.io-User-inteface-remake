const feed = document.querySelector('#feed');
const macroFeed = document.querySelector( '#macro-feed' );
const doublesplit = document.querySelector( '#doublesplit' );
const triplesplit = document.querySelector( '#triplesplit' );
const quadsplit = document.querySelector( '#quadsplit' );
const split32x = document.querySelector( '#split-32x' );
const split64x = document.querySelector( '#split-64x' );
const diagonalLinesplit = document.querySelector( '#diagonal-linesplit' );
const freezeMouse = document.querySelector( '#freeze-mouse' );
const lockLinesplit = document.querySelector( '#lock-linesplit' );
const stopMovement = document.querySelector( '#stop-movement' );
const respawn = document.querySelector( '#respawn' );
const toggleAutoRespawn = document.querySelector( '#toggle-auto-respawn' );
const toggleNames = document.querySelector( '#toggle-names' );
const toggleMass = document.querySelector( '#toggle-mass' );
const toggleSkins = document.querySelector( '#toggle-skins' );
const toggleFood = document.querySelector( '#toggle-food' );
const toggleChat = document.querySelector( '#toggle-chat' );
const toggleHUD = document.querySelector( '#toggle-hud' );
const spectateLock = document.querySelector( '#spectate-lock' );
const saveReplay = document.querySelector( '#save-replay' );

feed.addEventListener('keydown', e => {

	    if (feed.value.length >= 1) {
	        feed.value = e.key;
	    }
});

macroFeed.addEventListener('keydown', e => {

	    if (macroFeed.value.length >= 1) {
	        macroFeed.value = e.key;
	    }
});

doublesplit.addEventListener('keydown', e => {

	    if (doublesplit.value.length >= 1) {
	        doublesplit.value = e.key;
	    }
});

triplesplit.addEventListener('keydown', e => {

	    if (triplesplit.value.length >= 1) {
	        triplesplit.value = e.key;
	    }
});

quadsplit.addEventListener('keydown', e => {

	    if (quadsplit.value.length >= 1) {
	        quadsplit.value = e.key;
	    }
});

split32x.addEventListener('keydown', e => {

	    if (split32x.value.length >= 1) {
	        split32x.value = e.key;
	    }
});

split64x.addEventListener('keydown', e => {

	    if (split64x.value.length >= 1) {
	        split64x.value = e.key;
	    }
});

diagonalLinesplit.addEventListener('keydown', e => {

	    if (diagonalLinesplit.value.length >= 1) {
	        diagonalLinesplit.value = e.key;
	    }
});

freezeMouse.addEventListener('keydown', e => {

	    if (freezeMouse.value.length >= 1) {
	        freezeMouse.value = e.key;
	    }
});

lockLinesplit.addEventListener('keydown', e => {

	    if (lockLinesplit.value.length >= 1) {
	        lockLinesplit.value = e.key;
	    }
});

stopMovement.addEventListener('keydown', e => {

	    if (stopMovement.value.length >= 1) {
	        stopMovement.value = e.key;
	    }
});

respawn.addEventListener('keydown', e => {

	    if (respawn.value.length >= 1) {
	        respawn.value = e.key;
	    }
});

toggleAutoRespawn.addEventListener('keydown', e => {

	    if (toggleAutoRespawn.value.length >= 1) {
	        toggleAutoRespawn.value = e.key;
	    }
});

toggleNames.addEventListener('keydown', e => {

	    if (toggleNames.value.length >= 1) {
	        toggleNames.value = e.key;
	    }
});

toggleMass.addEventListener('keydown', e => {

	    if (toggleMass.value.length >= 1) {
	        toggleMass.value = e.key;
	    }
});

toggleSkins.addEventListener('keydown', e => {

	    if (toggleSkins.value.length >= 1) {
	        toggleSkins.value = e.key;
	    }
});

toggleFood.addEventListener('keydown', e => {

	    if (toggleFood.value.length >= 1) {
	        toggleFood.value = e.key;
	    }
});

toggleChat.addEventListener('keydown', e => {

	    if (toggleChat.value.length >= 1) {
	        toggleChat.value = e.key;
	    }
});

toggleHUD.addEventListener('keydown', e => {

	    if (toggleHUD.value.length >= 1) {
	        toggleHUD.value = e.key;
	    }
});

spectateLock.addEventListener('keydown', e => {

	    if (spectateLock.value.length >= 1) {
	        spectateLock.value = e.key;
	    }
});


saveReplay.addEventListener('keydown', e => {

	    if (saveReplay.value.length >= 1) {
	        saveReplay.value = e.key;
	    }
});